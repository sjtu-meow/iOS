//
//  ViewHistorySourceTests.swift
//  Meow
//
//  Created by 唐楚哲 on 2017/7/18.
//  Copyright © 2017年 喵喵喵的伙伴. All rights reserved.
//

import XCTest
@testable import Meow

class ViewHistorySourceTests : XCTestCase {
    
    /*
    func testEqual() {
        let item1 = ViewHistoryItem(id: 1, type: .article)
        let item2 = ViewHistoryItem(id: 1, type: .article)
        let item3 = ViewHistoryItem(id: 2, type: .article)
        let item4 = ViewHistoryItem(id: 1, type:.answer)
        XCTAssertEqual(item1, item2)
        XCTAssertNotEqual(item1, item3)
        XCTAssertNotEqual(item1, item4)
    }
 */
    /*
    func testAddHistory() {
        // item to add
        let item = ViewHistoryItem(id: 1, type: .article)
        
        // add and test
        ViewHistorySource.clearHistory()
        ViewHistorySource.addHistory(historyItem: item)
        let history = ViewHistorySource.getHistory()
        XCTAssertEqual(1, history.count)
        
        XCTAssertEqual(history[0], item)
        //XCT
        
        // delete and test
        ViewHistorySource.clearHistory()
        XCTAssertEqual(0, ViewHistorySource.getHistory().count)
        
    }

 */
 }
